@startuml
title Діаграма діяльності — Розрахунок середнього балу

start
:Введення оцінки;
if (Оцінка коректна?) then (так)
  :Додати оцінку до списку;
  if (Додати ще одну оцінку?) then (так)
    :Введення оцінки;
  else (ні)
    :Розрахувати середній бал;
    if (Список порожній?) then (так)
      :Показати попередження;
    else (ні)
      :Вивести середній бал;
    endif
  endif
else (ні)
  :Показати повідомлення про помилку;
endif
stop
@enduml
